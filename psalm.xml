<?xml version="1.0"?>
<psalm
    errorLevel="4"
    resolveFromConfigFile="true"
    findUnusedCode="false"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns="https://getpsalm.org/schema/config"
>
    <projectFiles>
        <directory name="src" />
    </projectFiles>

    <issueHandlers>
        <!--
            This is a library: its public API is "called" by consumers, not internally,
            so unused-code detection produces false positives. (findUnusedCode is also off.)
        -->
        <PossiblyUnusedMethod errorLevel="suppress" />
        <PossiblyUnusedProperty errorLevel="suppress" />
        <UnusedClass errorLevel="suppress" />

        <!-- #[\Override] on every interface implementation is noise we opt out of. -->
        <MissingOverrideAttribute errorLevel="suppress" />
    </issueHandlers>
</psalm>
